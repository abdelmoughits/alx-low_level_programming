#include <stdio>
/**
  *main - Prints its name, followed by a new line.
  *@argc: intiger varible
  *@argv: char array
  *Return: always 0 (sucese)
  */
int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
