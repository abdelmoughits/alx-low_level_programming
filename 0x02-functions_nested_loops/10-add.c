#include <stdio.h>
/**
  *add - Adds two integers and returns the result.
  *@a: a number of type intiger
  *@b: a number of type intiger
  *Return: the sume of to intigers
  */
int add(int a, int b)
{
	return (a + b);
}
