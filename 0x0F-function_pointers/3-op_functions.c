#include "3-calc.h"
/**
  *op_add - Calculat the sum
  *@a: intiger variable
  *@b: integer variable
  *Return: the sum of a and b
  */
int op_add(int a, int b)
{
	return (a + b);
}
/**
  *op_sub - Calculat the sub
  *@a: intiger variable
  *@b: integer variable
  *Return: the sub of a and b
  */
int op_sub(int a, int b)
{
	return (a - b);
}
/**
  *op_mul - Calculat the mul
  *@a: intiger variable
  *@b: integer variable
  *Return: the mul of a and b
  */
int op_mul(int a, int b)
{
	return (a * b);
}
/**
  *op_div - Calculat the div of and b
  *@a: intiger variable
  *@b: integer variable
  *Return: the div of a and b
  */
int op_div(int a, int b)
{
	return (a / b);
}
/**
  *op_mod - Calculat the mod of a and b
  *@a: intiger variable
  *@b: integer variable
  *Return: the mod betwin a and b
  */
int op_mod(int a, int b)
{
	return (a % b);
}
